<div class="card-trip">
  <% if movie.photos.attached? %>
    <%= cl_image_tag movie.photos.first.key, width: 304, height: 304, crop: :thumb, gravity: :face %>
  <% else %>
    <%= cl_image_tag("unknown_mn7pzj.png", :width=>304, :crop=>"scale") %>
  <% end %>
  <div class="card-trip-infos">
    <div>
      <h2><%= movie.title %></h2>
      <div class="list-group">
        <small>
          <% count = 0 %>
          <% 5.times do %>
            <% if movie.rating.round > count %>
              <i class="fas fa-star"></i>
            <% else %>
              <i class="far fa-star"></i>
            <% end %>
            <% count += 1 %>
          <% end %>
        </small>
      </div>
    </div>
  <p class="card-trip-pricing">From <%= movie.showtimes.map { |showtime| showtime.price_per_seat }.min %> €</p>
  </div>
</div>
