<!-- <div class="swiper-container">
    <div class="swiper-wrapper">
      <div class="swiper-slide">Slide 1</div>
      <div class="swiper-slide">Slide 2</div>
      <div class="swiper-slide">Slide 3</div>
      <div class="swiper-slide">Slide 4</div>
      <div class="swiper-slide">Slide 5</div>
      <div class="swiper-slide">Slide 6</div>
      <div class="swiper-slide">Slide 7</div>
      <div class="swiper-slide">Slide 8</div>
      <div class="swiper-slide">Slide 9</div>
      <div class="swiper-slide">Slide 10</div>
    </div>
    <div class="swiper-pagination"></div>
  </div> -->

<!-- <div class="swiper-container">
   <div class="swiper-wrapper">
    <% @movies.each do |movie| %>
       <div class="swiper-slide">
          <%= cl_image_tag movie.photos.first.key %>
       </div>
    <% end %>
  </div>
  <div class="swiper-pagination"></div>
</div>
 -->

<!-- <div class="swiper-container">
  <h2>Best Deals</h2>
  <% @movies.each do |movie| %>
      <div class="swiper-wrapper">
        <%= link_to movie_path(movie) do %>
          <% if movie.photos.attached? %>
            <div class="swiper-slide">
              <%= cl_image_tag movie.photos.first.key %>
            </div>
          <% else %>
            <div class="swiper-slide">
              <%= cl_image_tag("unknown_mn7pzj.png", :width=>304, :crop=>"scale") %>
            </div>
          <% end %>
        </div>
      <% end %>
    <% end %>
</div> -->

<div class="container-index">
  <!-- Best Deals -->
  <h2>Meilleures offres</h2>
  <div class="siema">
    <% @movies.each do |movie| %>
      <% if movie.almost_finished? %>
      <!-- Carrousel for each best deals card -->
          <div><%= render './shared/card_index', movie: movie %></div>
      <% end %>
    <% end %>
  </div>

<!-- test -->
  <!-- Category list -->
  <% @categories.each do |category| %>
    <h2><%= category %></h2>
      <div class="siema">
      <% @movies.where(category: category).each do |movie| %>
          <div><%= render './shared/card_index', movie: movie %></div>
      <% end %>
    </div>
  <% end %>
</div>
